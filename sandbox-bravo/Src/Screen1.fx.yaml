Screen1 As screen:
    OnVisible: |+
        =Set(
            isSecurity,
            If(
                CountRows(Security) > 0,
                true,
                false
            )
        );
        

    modalSecurity_2 As groupContainer:
        Height: =768
        Visible: =isSecurity
        Width: =App.Width
        ZIndex: =1

